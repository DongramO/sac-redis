const ranking = {
  host: "test2.k5ujjq.0001.apn2.cache.amazonaws.com",
  port: 6379,
  db: 0, 
}

const shard = {
  host: "test2.k5ujjq.0001.apn2.cache.amazonaws.com",
  port: 6379,
  db: 0, 
}

const user = {
  host: "localhost",
  port: 6379,
  db: 0, 
}

const admin = {
  host: "test2.k5ujjq.0001.apn2.cache.amazonaws.com",
  port: 16301,
  db: 0, 
}

const migration = {
  host: "localhost",
  port: 6380,
  db: 0, 
}

module.exports = {
  ranking,
  shard,
  user,
  admin,
  migration,
}
